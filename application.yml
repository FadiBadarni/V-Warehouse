spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:website}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:admin}
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  thymeleaf:
    check-template-location: false



app:
  cors:
    allowed-origins: http://localhost:3000
  jwt:
    secret-key: 6A576E5A7234753778214125442A472D4B6150645367556B5870327335763879
    expiration-time-ms: 7200000

